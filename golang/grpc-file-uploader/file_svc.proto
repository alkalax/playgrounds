syntax = "proto3";

package filesvc;

option go_package = "./proto";

message UploadRequest {
  bytes data_chunk = 1;
}

message UploadResponse {
  string file_id = 1;
  int32 total_bytes_received = 2;
}

service FileService {
  rpc UploadFile(stream UploadRequest) returns (UploadResponse);
}
